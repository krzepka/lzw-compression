#include "Decoder.h"

Decoder::Decoder(const std::string& s):Coder(s){};

void Decoder::work(){
    std::ofstream out;
    std::string decoded = path;

    int previousIndex;
    char firstCharacter;
    out<<"nothing";
    out.close(); 
}